[package]
name = "Carrot"
edition = "2024"

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3

# Enable more optimization in the release profile at the cost of compile time.
[profile.release]
# Compile the entire crate as one unit.
# Slows compile times, marginal improvements.
codegen-units = 1
# Do a second optimization pass over the entire program, including dependencies.
# Slows compile times, marginal improvements.
lto = "thin"

[dependencies]
bevy = { version = "0.16" }
nestify = "0.3.3"
bevy-panic-handler = "5.0"
bevy_trenchbroom = { version = "0.9", features = ["rapier", "bsp"] }
bevy-inspector-egui = "0.33"
bevy_framepace = "0.19"
nil = "0.15.0"
# Version 0.31 of rapier makes the colliders generated by bevy_trenchbroom not work in a very bizzare way.
bevy_rapier3d = { version = "0.30" }
